{
  "name": "mern-boilerplate",
  "version": "1.0.0",
  "description": "Starting Point for MERN application",
  "scripts": {
    "start": "concurrently \"cd backend && nodemon server.js\" \"cd frontend && npm start\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^5.0.0"
  },
  "devDependencies": {
    "nodemon": "^1.19.4"
  }
}
